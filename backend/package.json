{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "The backend of Scott Hetrick's portfolio website.",
  "main": "src/server.ts",
  "scripts": {
    "start": "dotenvx run --env-file=../../../.env -- ts-node --transpile-only ./src/server.ts",
    "dev": "dotenvx run --env-file=../../../.env -- nodemon --exec  ts-node ./src/server.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "nodemon": "^3.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@repo/su-done-ku-backend": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
